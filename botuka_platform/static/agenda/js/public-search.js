document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-agenda-more]').forEach(button=>{button.addEventListener('click',()=>{const days=button.closest('.agenda-result-availability')?.querySelector('[data-agenda-days]');if(!days)return;days.classList.toggle('is-expanded');button.textContent=days.classList.contains('is-expanded')?'Mostrar menos horários':'Mostrar mais horários';});});});
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-agenda-autocomplete]').forEach(input=>{const box=input.closest('.agenda-autocomplete')?.querySelector('[data-agenda-suggestions]');let timer;const close=()=>{if(box)box.hidden=true;input.setAttribute('aria-expanded','false')};input.addEventListener('input',()=>{clearTimeout(timer);const query=input.value.trim();if(query.length<2){close();return}timer=setTimeout(async()=>{try{const response=await fetch(`${input.dataset.url}?q=${encodeURIComponent(query)}`,{headers:{Accept:'application/json'}});if(!response.ok)return close();const data=await response.json();box.replaceChildren(...data.sugestoes.map(item=>{const button=document.createElement('button');button.type='button';button.setAttribute('role','option');const label=document.createElement('strong');label.textContent=item.rotulo;const type=document.createElement('span');type.textContent=item.tipo;button.append(label,type);button.addEventListener('click',()=>{input.value=item.valor;close();input.form?.requestSubmit()});return button}));box.hidden=!data.sugestoes.length;input.setAttribute('aria-expanded',String(data.sugestoes.length>0))}catch(_error){close()}},220)});document.addEventListener('click',event=>{if(!input.closest('.agenda-autocomplete')?.contains(event.target))close()})})});
