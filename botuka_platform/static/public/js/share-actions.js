document.addEventListener("click",async event=>{const root=event.target.closest("[data-share-actions]");if(!root)return;const url=root.dataset.url,title=root.dataset.title;if(event.target.closest("[data-open-qr]"))root.querySelector("[data-qr-modal]").showModal();if(event.target.closest("[data-copy-link]")){await navigator.clipboard.writeText(url);event.target.textContent="Link copiado";}if(event.target.closest("[data-native-share]")){if(navigator.share)await navigator.share({title,url});else await navigator.clipboard.writeText(url);}});
