document.addEventListener("submit",function(event){const form=event.target,message=form.dataset.confirm;if(message&&!window.confirm(message)){event.preventDefault();return}if(form.matches("[data-news-form]")){form.querySelectorAll("button[type=submit]").forEach(button=>button.disabled=true)}});
document.addEventListener("input",function(event){document.querySelectorAll("[data-counter-for]").forEach(counter=>{const field=document.getElementById(counter.dataset.counterFor);if(field)counter.textContent=`${field.value.length}${field.maxLength>0?` / ${field.maxLength}`:""} caracteres`});if(event.target.matches("input[type=file]")&&event.target.files[0]){const preview=document.querySelector("[data-image-preview]");if(preview){const image=document.createElement("img");image.alt="Pré-visualização da imagem";image.src=URL.createObjectURL(event.target.files[0]);preview.replaceChildren(image)}}});
