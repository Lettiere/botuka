document.addEventListener("DOMContentLoaded",()=>{const modal=document.getElementById("panelConfirmModal"),button=document.getElementById("panelConfirmButton"),message=document.getElementById("panelConfirmMessage");if(!modal||!button)return;let target=null;document.addEventListener("click",event=>{const trigger=event.target.closest("[data-panel-confirm]");if(!trigger)return;target=trigger;message.textContent=trigger.dataset.panelConfirm||"Esta ação requer confirmação.";button.textContent=trigger.dataset.confirmLabel||"Confirmar";});button.addEventListener("click",()=>{if(!target)return;if(target.tagName==="A"){window.location.assign(target.href);return}const form=target.closest("form");if(form){target.removeAttribute("data-panel-confirm");form.requestSubmit(target)}})});
